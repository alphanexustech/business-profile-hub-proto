users_collection = 'users'
tasks_collection = 'tasks'
elements_collection = 'elements'

BCRYPT_LOG_ROUNDS = 4

# IDEA: Change this file server side, or better, load from a file server side
# The secret key must be random and only accessible server-side!
SECRET_KEY = 'not_currently_a_secret'
